const CLI = require('./lib/cli');
const colors = require('./lib/colors'); 
const {Shape, Circle, Triangle, Square} = require('./lib/shapes'); 
const cli = new CLI();

cli.run();
// This calls the run() method that starts the logo generation process
// by prompting the user input, collecting the answers and then generates the
// logo based on the user input.

        

